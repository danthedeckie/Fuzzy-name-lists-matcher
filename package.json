{
  "type": "module",
  "dependencies": {
    "esbuild": "^0.19.11",
    "esbuild-jest": "^0.5.0"
  },
  "scripts": {
    "esbuild": "esbuild",
    "test": "node --experimental-vm-modules ./node_modules/jest/bin/jest.js"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
